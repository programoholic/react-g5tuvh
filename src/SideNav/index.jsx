import React from 'react';

const SideNav = (props) => {
  return (
    <div>
      <button> Create </button>
      <ul>
        <li> work </li>
      </ul>
    </div>
  );
};

export default SideNav;
